require 'spec_helper'

describe PoroProperties do
  it 'has a version number' do
    expect(PoroProperties::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
